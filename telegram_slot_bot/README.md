# 🎰 Телеграм-бот: Однорукий бандит

Красочный слот-бот с классическими символами, линиями выплат и диким символом 🌟.

## Установка

1) Создайте и активируйте виртуальное окружение (рекомендуется):

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

2) Установите зависимости:

```powershell
pip install -r requirements.txt
```

3) Установите токен как переменную окружения (Windows PowerShell):

```powershell
setx TELEGRAM_TOKEN "ВАШ_ТОКЕН"
```

После этого перезапустите терминал/IDE, чтобы переменная подхватилась.

## Запуск

```powershell
python bot.py
```

Бот использует polling. Откройте ваш бот в Telegram и нажмите /start.

## Геймплей

- 3 барабана, сетка 3x3.
- Линии: 3 горизонтали + 2 диагонали.
- Символ 🌟 — дикий, заменяет любой для максимального выигрыша.
- Ставка регулируется кнопками ➖/➕, SPIN крутит барабаны.
- Таблица выплат (за 3 в линию, умножается на ставку):
  - 🍒 ×3 → 5x
  - 🍋 ×3 → 8x
  - 🍇 ×3 → 10x
  - 🔔 ×3 → 15x
  - 💎 ×3 → 25x
  - 7️⃣ ×3 → 50x

## Новые функции

- **Хранилище баланса (SQLite)**: баланс, статистика (ставки/выигрыши) сохраняются в `slot.db`.
- **Ежедневный бонус**: кнопка "🎁 Бонус" — +200 к балансу раз в 24 часа.
- **Таблица лидеров**: кнопка "🏆 Топ" — топ игроков по балансу.
- **Анимация спина**: короткая псевдо-анимация кручения барабанов перед результатом.

Файл базы данных `slot.db` создаётся в папке проекта автоматически.

## Примечание по безопасности

Не храните реальный токен в коде или репозитории. Используйте переменные окружения.
