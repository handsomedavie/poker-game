# 🎯 Best Hand Visualizer - Полная Реализация

## 📋 Обзор Функциональности

Реализована профессиональная система визуализации лучшей покерной комбинации для игрока в реальном времени. Система автоматически определяет лучшую 5-карточную комбинацию из карт игрока и общих карт на столе, подсвечивает эти карты золотым контуром с эффектом свечения и отображает название комбинации в нижнем левом углу экрана.

---

## 🎨 Реализованные Компоненты

### 1. **Hand Evaluator (handEvaluator.ts)**

Полноценная утилита для оценки покерных комбинаций:

#### Основные Функции:
- **`evaluateBestHand(cards: Card[])`** — находит лучшую 5-карточную комбинацию из 5-7 карт
- **`isCardInBestHand(card: Card, bestHand: EvaluatedHand | null)`** — проверяет, входит ли карта в лучшую комбинацию

#### Поддерживаемые Комбинации:
```typescript
type HandRank = 
  | 'Royal Flush'         // Роял-Флеш
  | 'Straight Flush'      // Стрит-Флеш
  | 'Four of a Kind'      // Каре
  | 'Full House'          // Фулл-Хаус
  | 'Flush'               // Флеш
  | 'Straight'            // Стрит
  | 'Three of a Kind'     // Сет/Трипс
  | 'Two Pair'            // Две пары
  | 'Pair'                // Пара
  | 'High Card';          // Старшая карта
```

#### Структура EvaluatedHand:
```typescript
interface EvaluatedHand {
  rank: HandRank;              // Название комбинации
  cards: Card[];               // 5 карт, входящих в комбинацию
  description: string;         // Описание (например, "Pair of Kings")
}
```

#### Алгоритм:
1. Генерирует все возможные 5-карточные комбинации из доступных карт
2. Оценивает каждую комбинацию согласно правилам покера
3. Возвращает лучшую комбинацию с указанием конкретных карт

**Особенности:**
- ✅ Правильная обработка "Wheel" straight (A-2-3-4-5)
- ✅ Точное сравнение одинаковых комбинаций по старшинству карт
- ✅ Корректная работа с 5-7 картами
- ✅ Используется стандартная покерная терминология (Straight вместо Street, Flush вместо Flesh)

---

### 2. **Card Component Updates**

Обновлен компонент `Card.tsx` для поддержки highlighted состояния:

```typescript
interface CardProps {
  rank: CardRank;
  suit: CardSuit;
  faceDown?: boolean;
  size?: 'small' | 'medium' | 'large';
  className?: string;
  highlighted?: boolean;  // 🆕 Новое поле
}
```

**Стили Highlighted Карт (`card.module.css`):**
```css
.highlighted {
  border: 3px solid #ffd700 !important;
  box-shadow: 
    0 0 20px rgba(255, 215, 0, 0.8),
    0 0 40px rgba(255, 215, 0, 0.5),
    0 0 80px rgba(255, 215, 0, 0.4);
  animation: golden-glow 2s ease-in-out infinite;
  transform: translateY(-4px);
  z-index: 10;
}

@keyframes golden-glow {
  0%, 100% { /* Стандартное свечение */ }
  50% { /* Усиленное свечение */ }
}
```

**Визуальные Эффекты:**
- 🟡 Золотой контур (border: 3px solid #ffd700)
- ✨ Пульсирующее свечение (golden-glow animation)
- ⬆️ Небольшой подъём карты (translateY: -4px)
- 🔝 Повышенный z-index для выделения

---

### 3. **HandStrengthHUD Component**

Новый компонент для отображения текущей комбинации игрока.

**Расположение:** Нижний левый угол экрана (bottom: 140px, left: 20px)

**Структура:**
```tsx
<HandStrengthHUD
  handRank="Pair"                  // Название комбинации
  description="Pair of Kings"      // Описание
/>
```

**Цветовая Схема (по редкости):**

| Комбинация | Цвет | Иконка | Класс |
|------------|------|--------|-------|
| Royal Flush / Straight Flush | 🔥 Оранжевый (#ff6b00) | 👑 / 💎 | legendary |
| Four of a Kind / Full House | 💜 Фиолетовый (#a335ee) | 🔥 / 🏠 | epic |
| Flush / Straight | 🔵 Синий (#0070dd) | ♠️ / 📊 | rare |
| Three of a Kind / Two Pair | 🟢 Зелёный (#1eff00) | 🎯 / 👥 | uncommon |
| Pair / High Card | ⚪ Серый (#9d9d9d) | 🎲 / 🃏 | common |

**Стиль:**
- Полупрозрачный чёрный фон (rgba(0, 0, 0, 0.85))
- Backdrop blur эффект
- Анимация появления (slideIn)
- Пульсирующая иконка (pulse animation)
- Адаптивная вёрстка для мобильных устройств

---

### 4. **PlayerSeat Component Updates**

Обновлён для поддержки highlighted карт:

```typescript
export interface PlayerSeatProps {
  // ... существующие поля
  highlightedCards?: Card[];  // 🆕 Карты для подсветки
}
```

**Логика Отображения:**
```typescript
{player.cards?.map((card, idx) => {
  const isHighlighted = highlightedCards?.some(
    (hc) => hc.rank === card.rank && hc.suit === card.suit
  ) ?? false;
  
  return (
    <Card
      rank={card.rank}
      suit={card.suit}
      highlighted={isHighlighted && (isCurrentUser || revealCards)}
    />
  );
})}
```

**Важно:** Подсветка отображается только для текущего игрока или при showdown!

---

### 5. **PokerTable Integration**

Интеграция Best Hand Visualizer в основной компонент стола:

#### Состояние:
```typescript
const [heroBestHand, setHeroBestHand] = useState<EvaluatedHand | null>(null);
```

#### Автоматическое Вычисление:
```typescript
useEffect(() => {
  const heroSeat = seats.find((s) => s.player?.id === currentUserId);
  if (!heroSeat?.player?.cards || communityCards.length < 3) {
    setHeroBestHand(null);
    return;
  }

  const combinedCards = [...heroSeat.player.cards, ...communityCards];
  const bestHand = evaluateBestHand(combinedCards);
  setHeroBestHand(bestHand);
}, [seats, currentUserId, communityCards]);
```

**Триггеры Обновления:**
- Изменение карт игрока
- Появление новых community cards
- Смена активного пользователя

#### Highlighted Cards в Community Cards:
```typescript
{communityCards.map((card, index) => {
  const isHighlighted = heroBestHand?.cards.some(
    (hc) => hc.rank === card.rank && hc.suit === card.suit
  ) ?? false;
  
  return (
    <CardComponent
      rank={card.rank}
      suit={card.suit}
      highlighted={isHighlighted}
      size="medium"
    />
  );
})}
```

#### Highlighted Cards в Hero Seat:
```typescript
<PlayerSeat
  position={heroSeat.position}
  player={heroSeat.player}
  currentUserId={currentUserId}
  highlightedCards={heroBestHand?.cards}
  // ... остальные props
/>
```

#### HUD Display:
```typescript
{heroBestHand && (
  <HandStrengthHUD
    handRank={heroBestHand.rank}
    description={heroBestHand.description}
  />
)}
```

---

## 🎮 Как Работает Система

### Сценарий 1: Флоп (3 общие карты)
1. Игрок получил **A♠ K♠**
2. Флоп: **Q♠ J♠ 10♥**
3. **Система вычисляет:** Straight (A-K-Q-J-10)
4. **Подсвечивает:** A♠, K♠, Q♠, J♠, 10♥ (золотым контуром)
5. **Отображает HUD:** 📊 **Straight** - "Straight, Ace high"

### Сценарий 2: Терн (4 общие карты)
1. Терн: **9♠**
2. **Система пересчитывает:** Flush (5 пиковых карт)
3. **Подсвечивает:** A♠, K♠, Q♠, J♠, 9♠
4. **Обновляет HUD:** ♠️ **Flush** - "Flush, Ace high"

### Сценарий 3: Ривер (5 общих карт)
1. Ривер: **A♣**
2. **Система пересчитывает:** Two Pair (A-A, Q-J-10 для стрита больше не хватает)
3. **Подсвечивает:** A♠, A♣, K♠, Q♠, J♠
4. **Обновляет HUD:** 👥 **Two Pair** - "Two Pair, Aces and Kings"

---

## 🎯 Ключевые Особенности

### 1. **Динамическое Обновление**
- ✅ Автоматический пересчёт при каждом изменении карт
- ✅ Мгновенное обновление highlighted состояния
- ✅ Плавные анимации и переходы

### 2. **Только для Текущего Игрока**
- ✅ Визуализация отображается только для героя
- ✅ Противники не видят подсветку карт героя
- ✅ На showdown подсветка показывается всем

### 3. **Точность Алгоритма**
- ✅ Правильное определение всех комбинаций
- ✅ Корректная работа с 5-7 картами
- ✅ Учёт всех граничных случаев (wheel straight, ace-high vs ace-low)

### 4. **UI/UX**
- ✅ Понятное отображение названия комбинации
- ✅ Интуитивные цветовые схемы по редкости
- ✅ Красивые анимации и эффекты
- ✅ Адаптивный дизайн

---

## 📱 Адаптивность

Система полностью адаптирована для мобильных устройств:

```css
@media (max-width: 768px) {
  .hudContainer {
    bottom: 120px;
    left: 10px;
    padding: 10px 14px;
    min-width: 180px;
  }
  
  .hudIcon { font-size: 1.5em; }
  .hudRank { font-size: 1em; }
  .hudDescription { font-size: 0.75em; }
}
```

---

## 🚀 Как Запустить

### 1. Перезапусти Бэкенд (если нужно)
```powershell
cd c:\Users\DAVIE\Desktop\windsurf ai\poker
uvicorn server:app --reload
```

### 2. Запусти Фронтенд
```powershell
cd c:\Users\DAVIE\Desktop\windsurf ai\poker\poker-table-ui
npm start
```

### 3. Открой Игру
```
http://localhost:3000
```

---

## 🧪 Тестирование

### Тест 1: Pair
1. Получи карты с парой (например, K♠ K♦)
2. **Проверь:** Обе карты с парой подсвечены золотом
3. **Проверь HUD:** "Pair of Kings"

### Тест 2: Flush
1. Получи 5 карт одной масти на столе
2. **Проверь:** 5 карт масти подсвечены
3. **Проверь HUD:** "Flush, X high"

### Тест 3: Full House
1. Получи 3 одинаковые + 2 одинаковые карты
2. **Проверь:** Все 5 карт подсвечены
3. **Проверь HUD:** "Xs full of Ys"

### Тест 4: Динамическое Обновление
1. Наблюдай как подсветка меняется при появлении новых community cards
2. **Проверь:** HUD обновляется мгновенно
3. **Проверь:** Анимация плавная и красивая

---

## 📊 Структура Файлов

```
poker-table-ui/src/
├── utils/
│   └── handEvaluator.ts                    # 🆕 Алгоритм оценки комбинаций
├── components/
│   ├── cards/
│   │   ├── Card.tsx                        # ✏️ Обновлён (highlighted prop)
│   │   └── card.module.css                 # ✏️ Обновлён (highlighted styles)
│   ├── HandStrengthHUD/
│   │   ├── HandStrengthHUD.tsx             # 🆕 HUD компонент
│   │   └── hand_strength_hud.module.css    # 🆕 Стили HUD
│   └── PokerTable/
│       ├── PokerTable.tsx                  # ✏️ Обновлён (интеграция)
│       └── PlayerSeat.tsx                  # ✏️ Обновлён (highlightedCards)
└── types/
    └── player.types.ts                     # ✏️ Обновлён (highlightedCards в Props)
```

---

## ✅ Чек-Лист Реализации

### Алгоритм:
- ✅ Правильное определение всех 10 комбинаций
- ✅ Корректная работа с 5-7 картами
- ✅ Wheel straight (A-2-3-4-5) обрабатывается верно
- ✅ Возврат конкретных 5 карт комбинации

### Визуализация:
- ✅ Золотой контур (border: 3px solid #ffd700)
- ✅ Пульсирующее свечение (golden-glow animation)
- ✅ Подсветка только для текущего игрока
- ✅ Динамическое обновление при изменении карт

### HUD:
- ✅ Отображение названия комбинации
- ✅ Описание комбинации
- ✅ Цветовая схема по редкости
- ✅ Иконки для каждой комбинации
- ✅ Анимация появления и пульсация
- ✅ Адаптивный дизайн

### Интеграция:
- ✅ Highlighted карты в hero seat
- ✅ Highlighted карты в community cards
- ✅ Автоматическое вычисление при изменениях
- ✅ Правильная работа с remote control
- ✅ Showdown поддержка

---

## 🎉 Итог

Полностью реализована профессиональная система **Best Hand Visualizer** для покерного стола:

1. **Hand Evaluation** — Точный алгоритм оценки всех покерных комбинаций
2. **Visual Feedback** — Золотой контур и свечение на картах лучшей комбинации
3. **UI HUD** — Информативное отображение названия комбинации с цветовой кодировкой
4. **Multiplayer Context** — Визуализация только для текущего игрока (локальный рендер)

**Система готова к использованию и полностью соответствует требованиям!** 🚀
