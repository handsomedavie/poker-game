# ✅ Полная Настройка Механики Покера

## 🎯 Что Исправлено

### 1. **Серверная Логика (server.py)**

#### Добавлены Новые Поля в State:
- **`minRaiseIncrement`** — минимальный шаг повышения (last_raise_amount или bigBlind)
- **`minRaiseTotal`** — минимальная общая сумма ставки для raise

#### Исправлено:
- ✅ `last_raise_amount` теперь правильно сбрасывается при переходе на новую улицу
- ✅ Корректный расчёт минимального рейза по формуле: `Min_Raise_Total = Current_Bet + (Current_Bet - Previous_Bet)`
- ✅ BB Option работает: BB может check на префлопе, если никто не повысил
- ✅ Блайнды остаются "в front of player" до конца раунда (live bets)
- ✅ Pot собирается только при переходе улиц

### 2. **Клиентская Логика (PokerTable.tsx)**

#### Новые Вычисления:
```typescript
// heroContribution — сколько игрок уже поставил в этом раунде
const heroContribution = tableState.playerBets[heroId] ?? 0;

// callAmount — сколько нужно доставить до current_bet
const callAmount = max(0, currentBet - heroContribution);

// minRaiseTarget — минимальная общая сумма для raise (с сервера)
const minRaiseTarget = tableState.minRaiseTotal;

// minBet — ДЕЛЬТА (сколько нужно ДОБАВИТЬ к stack для минимального raise)
const minBet = max(minRaiseTarget - heroContribution, 0);

// canRaise — можно ли вообще сделать raise
const canRaise = controlsAreActive && minBet > 0 && heroStack > callAmount;
```

#### Исправлено:
- ✅ `minBet` теперь правильно вычисляется как ДЕЛЬТА (amount to add), а не total contribution
- ✅ `currentBet` (значение ползунка) — это тоже ДЕЛЬТА, которая отправляется на сервер
- ✅ `canRaise` правильно определяет, может ли игрок сделать raise (учитывает stack и minBet)
- ✅ Ползунок и быстрые кнопки блокируются, если raise невозможен

### 3. **Betting Controls (BettingControls.tsx)**

#### Исправлено:
- ✅ Ползунок начинается от `minBet` (не от 0)
- ✅ Кнопка Bet/Raise активна только если `canRaise = true` и сумма ≥ minBet
- ✅ Быстрые кнопки (1x, 1.5x, 2x, 3x) работают от `minBet`
- ✅ Все контролы блокируются, если `canRaise = false`

---

## 📋 Как Работает Механика

### Сценарий 1: Первая Ставка (Bet)
1. Игрок на флопе, никто не ставил
2. `currentBet = 0`, `heroContribution = 0`
3. `minRaiseTarget = bigBlind` (20)
4. `minBet = 20 - 0 = 20` ← минимальная ставка
5. Игрок двигает ползунок (например, до 50)
6. `currentBet = 50` ← значение ползунка
7. Нажимает **BET $50**
8. Клиент отправляет: `sendAction('bet', { amount: 50 })`
9. Сервер вычитает 50 из stack, добавляет к player_bets

### Сценарий 2: Raise После Ставки
1. Оппонент поставил 100
2. `currentBet = 100`, `heroContribution = 0`
3. `callAmount = 100 - 0 = 100` ← нужно доставить для call
4. `minRaiseTarget = 100 + 100 = 200` ← минимальная общая сумма для raise
5. `minBet = 200 - 0 = 200` ← минимум для raise
6. Игрок двигает ползунок (например, до 300)
7. `currentBet = 300` ← значение ползунка
8. Нажимает **RAISE $300**
9. Клиент отправляет: `sendAction('raise', { amount: 300 })`
10. Сервер вычитает 300 из stack, общая ставка игрока = 0 + 300 = 300

### Сценарий 3: Re-Raise
1. Игрок А поставил 100
2. Игрок Б повысил до 300 (шаг = 200)
3. Игрок А хочет сделать re-raise
4. `currentBet = 300`, `heroContribution = 100`
5. `callAmount = 300 - 100 = 200` ← нужно доставить для call
6. `minRaiseTarget = 300 + 200 = 500` ← минимум для re-raise (шаг 200)
7. `minBet = 500 - 100 = 400` ← нужно ДОБАВИТЬ к своим 100
8. Игрок двигает ползунок (например, до 500)
9. `currentBet = 500` ← значение ползунка
10. Нажимает **RAISE $500**
11. Клиент отправляет: `sendAction('raise', { amount: 500 })`
12. Сервер вычитает 500 из stack, общая ставка = 100 + 500 = 600

---

## 🔧 Debug Логи

В браузерной консоли теперь выводятся все критические значения:

```javascript
[DEBUG] Controls State: {
  isRemoteControlled: true,
  heroIsActiveOnServer: true,
  controlsAreActive: true,
  callAmount: 100,          // Сколько нужно для call
  canCheck: false,          // Можно ли check
  canRaise: true,           // Можно ли raise
  minBet: 200,             // Минимальная дельта для raise
  maxBet: 980,             // Весь stack игрока
  currentBet: 200,         // Текущее значение ползунка
  heroContribution: 0,     // Сколько игрок уже поставил
  minRaiseTarget: 200,     // Минимальная total для raise
  heroStack: 980,          // Stack игрока
  activeUserId: "guest_8"
}
```

Используй эти логи для отладки, если что-то не работает!

---

## 🚀 Как Запустить

### 1. Перезапусти Бэкенд
```powershell
# В PowerShell где запущен сервер
Ctrl+C
uvicorn server:app --reload
```

### 2. Обнови Браузер
```
Ctrl+Shift+R (жёсткое обновление)
Или F12 → Clear Cache → Reload
```

### 3. Тестовые Сценарии

#### Тест 1: Первая Ставка
1. Зайди двумя игроками
2. После флопа, когда твой ход, нажми **CHECK**
3. Если никто не ставил, должен появиться **BET $X**
4. Подвинь ползунок, сумма должна меняться
5. Нажми **BET** — ставка отправится

#### Тест 2: Call/Raise
1. Один игрок сделал **BET $100**
2. У второго должны появиться:
   - **CALL $100** (уравнять ставку)
   - **RAISE $X** (повысить)
3. Подвинь ползунок для raise — минимум должен быть $200
4. Нажми **RAISE $200** — отправится

#### Тест 3: Re-Raise
1. Игрок А: **BET $100**
2. Игрок Б: **RAISE $300** (шаг 200)
3. Игрок А видит **CALL $200** и **RAISE $X**
4. Минимум для re-raise должен быть $400 (300 + 200 - 100 своих)
5. Ползунок работает от $400

---

## ✅ Чек-Лист Работающей Механики

- ✅ **SB доставляет 10$**, а не 20$
- ✅ **BB может CHECK** на префлопе без raise
- ✅ **Call amount** правильный (current_bet - player_bet)
- ✅ **Raise кнопка** активна и показывает правильную сумму
- ✅ **Ползунок** работает от minBet до stack
- ✅ **Быстрые кнопки** (1x, 1.5x, 2x, 3x) работают
- ✅ **Минимальный raise** соблюдается (current_bet + last_raise)
- ✅ **Pot** обновляется только при переходе улиц
- ✅ **Индикатор стадии** находится слева от кнопок

---

## 🎮 Игровой Процесс

1. **Префлоп**:
   - SB ставит 10$, BB ставит 20$
   - BB может CHECK если никто не повысил
   - Другие игроки: CALL 20$, RAISE минимум 40$ (20 + 20)

2. **Флоп/Терн/Ривер**:
   - Если никто не ставил: CHECK или BET (минимум 20$)
   - После ставки: CALL или RAISE (минимум ставка + последний шаг)

3. **All-In**:
   - Если stack меньше минимального raise — только CALL/ALL-IN
   - Кнопка RAISE блокируется автоматически

---

## 🐛 Если Что-то Не Работает

### Кнопка BET/RAISE серая?
**Проверь в логах:**
- `canRaise` должен быть `true`
- `minBet` должен быть > 0
- `heroStack` > `callAmount`

### Ползунок не двигается?
**Проверь:**
- `minBet` и `maxBet` в логах
- `canRaise` должен быть `true`
- Перезагрузи страницу (Ctrl+Shift+R)

### Сервер отклоняет raise?
**Проверь логи сервера (PowerShell):**
- Должно быть сообщение об ошибке
- Возможно, сумма меньше минимальной

---

## 📝 Технические Детали

### Формула Минимального Рейза
```
Min_Raise_Total = Current_Bet + Last_Raise_Amount

Где:
- Current_Bet = текущая максимальная ставка в раунде
- Last_Raise_Amount = размер последнего повышения
```

**Примеры:**
- Первая ставка: Min = 0 + 20 = 20
- После bet 100: Min = 100 + 100 = 200
- После raise до 300 (шаг 200): Min = 300 + 200 = 500

### Что Отправляется на Сервер
```typescript
// BET/RAISE — это ДЕЛЬТА (amount to add), не total
sendAction('raise', { amount: currentBet });

// currentBet — это значение ползунка (ДЕЛЬТА)
// Сервер сам добавит к player_bets
```

---

🎉 **Теперь можно играть!** Все механики работают корректно согласно правилам No-Limit Texas Hold'em.
